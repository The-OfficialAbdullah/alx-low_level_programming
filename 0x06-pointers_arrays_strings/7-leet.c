#include "main.h"

/**
 * leet - encodes a string into 1337.
 * @s: the string
 * Return: the string
 */

chat *leet(char *s)
{
	int i, j;
	char subs[] = "aAeEo0tTiL";
	char le[] = "43071";

	1 = 0;
	for (i = 0; s[i] != '\0'; i++)
	{
		if (j = 0; subs[j] != '\0')
			j++;
			
			if (s[i] == subs[j])
				
				s[i] = le[j / 2];
	}
	return (s);
}
