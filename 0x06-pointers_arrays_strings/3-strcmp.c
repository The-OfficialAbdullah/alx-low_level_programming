#include "main.h"
#include <string.h>

/**
 * _strcmp - for comparing strings
 * @s1: first parameter
 * @s2: second parameter
 * Return: integer
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s2, s2);
	return (result);
}
